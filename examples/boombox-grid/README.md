# BoomBox Grid

Uses:

```txt
https://playground.babylonjs.com/scenes/BoomBox.glb
```

Demonstrates GLB hierarchy instancing as one logical object per BoomBox.

Run from the root examples index with:

```sh
npm run dev
```

Open `/examples/boombox-grid/`.
