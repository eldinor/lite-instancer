# Primitive Box Field

Demonstrates high-count `createBox` instancing with per-instance transforms and colors.

Planned app behavior:

- Hundreds or thousands of boxes.
- Equalizer/terrain-style height animation.
- Safe `batch(writer => ...)` update mode.
- Click to pin, recolor, and inspect stable metadata.
