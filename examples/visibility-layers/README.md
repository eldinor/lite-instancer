# Visibility Layers

Demonstrates visibility strategies.

Planned app behavior:

- Toggle colored primitive groups.
- Compare `visibleStrategy: "active-count"` and `"scale-zero"`.
- Keep selected hidden IDs alive across hide/show.
- Debug panel shows `count`, `visibleCount`, and selected metadata.
