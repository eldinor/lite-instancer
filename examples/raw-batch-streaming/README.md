# Raw Batch Streaming

Demonstrates `batch` and controlled `editRaw` for high-frequency updates.

Planned app behavior:

- Thousands of procedural primitive instances.
- Safe writer mode with `batch`.
- Performance mode with `editRaw`.
- Picking and stable IDs continue working during streaming updates.
