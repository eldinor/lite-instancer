# Xbot Basic Animation

Loads the supplied animated Xbot GLB into a basic Babylon Lite scene and loops each animation group found in the file.

Asset:

```txt
https://raw.githubusercontent.com/eldinor/bp900/main/public/model/Xbot.glb
```

Run `npm run dev`, then open `/examples/xbot-basic-animation/`.
