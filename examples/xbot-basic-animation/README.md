# Ready Player Basic Animation

Loads the supplied animated Ready Player GLB into a basic Babylon Lite scene and loops each animation group found in the file.

Asset:

```txt
https://raw.githubusercontent.com/eldinor/ForBJS/master/all-anim.glb
```

Run `npm run dev`, then open `/examples/xbot-basic-animation/`.
